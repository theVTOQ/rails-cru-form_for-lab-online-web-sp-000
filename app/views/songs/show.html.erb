<h1><%= @song.name %></h1>
<% artist = @song.artist %>
<% genre = @song.genre %>

<h2>Artist: <%= link_to artist.name, artist_path(artist) %></h2>
<h3>Genre: <%= link_to genre.name, genre_path(genre) %></h3>
