<h2>Create Artist</h2>
<%= form_for(@artist) do |f| %>
  <label>Name:</label>
  <%= f.text_field :name %>

  <label>Bio:</label>
  <%= f.text_field :bio %>

  <%= f.submit "Create Artist" %>
<% end %>
